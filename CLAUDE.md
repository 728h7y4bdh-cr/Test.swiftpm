# CLAUDE.md

このファイルは、本リポジトリ（Sample App）で作業するすべてのAIセッションが、作業開始時に必ず読み込むべき永続的な指示ファイルである。チャット履歴に依存せず、このファイル1つを読めば過去の開発方針を再現できることを目的とする。

## 0. このプロジェクトについて

- **Sample App**：iPad／iPhoneを1:1でBluetooth接続し、データ送受信を行う検証用アプリ。
- 開発ツールは**Swift Playgrounds**（Xcodeは使用しない）。UIフレームワークは**UIKit**（SwiftUIは使用しない）。
- 唯一の一次情報源は `docs/design/要件定義書.md`。すべての設計書（PS/SS/UI設計書）・コーディング規約・ソースコードのコメントは、この要件定義書の章番号を引用している。

## 1. まず読むべきドキュメント（この順序で）

1. `docs/design/要件定義書.md` — 何を作るかの一次情報
2. `docs/design/PS設計書.md` — 内部処理仕様（状態遷移、通信フォーマット、クラス構成）
3. `docs/design/SS設計書.md` — 画面仕様（ボタン操作、ダイアログ文言、処理順序）
4. `docs/design/UI設計書.md` — 画面レイアウト
5. `docs/コーディング規約.md` — 命名規則・アーキテクチャ方針・ファイル構成ルール
6. `docs/操作説明書.md` — エンドユーザー向け操作手順（実装した挙動の答え合わせに使える）
7. `docs/開発環境書.md` — 開発者・試験担当者向けの環境構築／ビルド／マージ／E2E試験環境の手順（本ファイルと一部重複するため、変更する場合は両方の整合性を確認すること）

**コード・設計書のレビューを依頼された場合は、必ず`docs/レビュー観点.md`を参照し、そこに定めた役割・観点に従うこと。** レビューのたびに観点がぶれないようにするための恒久的な基準であり、チャットの流れだけで自己流の観点を作らない。

**要件に関わる変更・追加を行う場合は、要件定義書の更新が必要かどうかを都度確認すること。** 更新が必要と判断した場合も、要件定義書を無断で直接書き換えるのではなく、まずユーザーへ変更案をサジェストし、承認を得てから要件定義書→PS/SS/UI設計書→ソースコードの順に反映する（詳細は2章の運用ルールを参照）。ソースコードだけを直して設計書を放置することも避ける。

## 2. 変更の進め方に関する運用ルール（最重要）

このプロジェクトはこれまで、ユーザーから「まず報告してください、修正はしないでください」という指示のもとで多くの変更を進めてきた。この運用姿勢を、ユーザーから明示的な指示がなくても既定の振る舞いとして維持すること。

- **依頼された範囲を超えて、既存コードを自動的にリファクタリング・再設計しない。** 微調整・小さな機能追加の依頼に対して、無関係な箇所まで書き直さない。
- **変更が「動作上の問題を起こしうる」または「コーディング規約に違反する」と判断した場合は、黙って直さず、まずその旨を報告し、ユーザーの指示を待つこと。** これは本プロジェクトで繰り返し使われてきた明示的なルールである。
- 設計書の記述に誤り・矛盾を見つけた場合、依頼された変更のついでに無断で直すのではなく、気づいた時点で報告する（軽微な誤字等、依頼作業と直接同じ箇所を触る場合を除く）。
- 大きな設計判断（例：処理の分割単位、命名パターンの新設、章立ての変更）を行う場合は、判断の根拠・別の選択肢との比較をユーザーに提示し、確認を仰ぐこと。既成事実として押し通さない。
- 1回の依頼に対する差分は、依頼内容に対して必要最小限に保つこと。「ついでに直しておいた」を避ける。
- **`docs/design/要件定義書.md`は特に慎重に扱う。** すべての設計書・コードの一次情報であるため、AIが良かれと思って自動的に更新し続けることは絶対に行わない。変更・追記が必要だと判断した場合は、常にその必要性をユーザーに確認し、まず変更案をサジェストすること。ユーザーの承認が得られない限り、要件定義書自体は修正しない。

## 3. アーキテクチャ・命名規則

`docs/コーディング規約.md` に従うこと。特に以下は繰り返し重要視されてきた方針である。

- **役割（何で通信するか）と機能（何をするか）の分離**：`BluetoothCentralSession`/`BluetoothPeripheralSession`（役割レイヤー）と、`ConnectionStartHandshake`/`ListenStartHandshake`/`Disconnector`/`DataSender`/`DataReceiver`（機能レイヤー）を混在させない。
- **状態遷移の一元管理**：状態（`AppStatus`）を書き換えられるのは `StatusManager.apply(_:)` 経由のみ。個々の処理が直接ステータスを代入しない。
- **1クラス1責務**：ただし、要件定義書上のごく小さな処理（例：`AppInitializer`）まで機械的に新規クラス化すると、将来的にファイル数だけが増えていくリスクがある。新しい処理を追加する際は、「本当に専用クラスが必要か」「既存クラスへの追加で十分か」を都度検討すること（詳細は2026-08-15のレビュー記録を参照。このレビュー内容自体は本ファイルには転記していないため、疑問があれば `docs/design/PS設計書.md` の改訂履歴・関連コミットログを確認すること）。
- **すべての型・主要メソッドに`///`コメントで対応する設計書の章番号を明記する**（トレーサビリティ）。

## 4. Package.swift とルートディレクトリ構成について

- リポジトリ直下（プロジェクトルート）は `Package.swift` / `README.md` / `Sources/` / `docs/` / `CLAUDE.md` のみに保つ方針。
- **`Package.swift` はSwiftPM／Swift Playgroundsのマニフェストファイルであり、必ずプロジェクトルート直下に置く必要がある。** `Sources/`配下など別の場所へ移動するとプロジェクト自体がビルド不能になるため、絶対に移動しないこと。
- `Package.swift`の内容（`.iOSApplication`宣言、`capabilities`、`supportedInterfaceOrientations`等）については、現時点では設計書側に説明がない。変更する場合は、可能であれば要件定義書またはPS設計書側にも根拠を残すことが望ましい。

## 5. このセッション（クラウド環境）でのビルド確認について

- **この実行環境には Swift ツールチェインがインストールされていない**（`swift build` 等のコマンドは使用不可）。実際のビルド確認は、ユーザー側のSwift Playgrounds環境でのみ可能。
- そのため、コードの正しさはコンパイラに頼らず、型・シグネチャ・呼び出し関係を目で追って確認すること。特に以下は要注意：
  - アクセス修飾子（`private`のまま他ファイルから呼べなくなっていないか）
  - クロージャのキャプチャ（`[weak self]`の付け忘れによる循環参照）
  - Optional周りの強制アンラップ
- 変更後は、関連するコメント（`///`の章番号）・設計書の記述・コーディング規約との整合性を必ず確認すること。

## 6. 調査用の一時的なログについて

不具合調査のためにコンソールログ等の調査用コードを追加する場合、**必ず`// TEMP-LOG`というコメントタグを同じ行に付け、後から`grep`等で一括して見つけて削除できる状態にすること。** 調査が終わったら、原因を報告した上で、このタグの付いたコードを全て削除する（ユーザーの明示的な指示なしに調査用ログを残したままにしない）。これは特定の調査に限らず、今後同様の調査を行う場合に常に適用する恒久的な運用ルールである。

## 7. 試験成果物について

`単体試験結果.md` / `統合試験結果.md` / `E2E試験結果.md`（要件定義書13章で言及）は、本ファイル作成時点で**未着手**。自動テストの仕組みも存在しない。テスト関連の作業を依頼された場合は、まずこの前提から説明すること。

## 8. 言語

ユーザーとのやり取り・ドキュメント・コードコメントはすべて日本語で統一する。

## 9. コード解説（学習目的の説明）について

- **適用条件**：以下の2パターンを基本とする。
  1. コードの説明を頼まれた時
  2. 修正内容を説明する時
- **説明範囲**：ユーザーが指定した範囲に限定して行う。自己判断で範囲を広げない。
- **留意点**：以下の説明方針は、ユーザーとの対話を通じて今後も改善され続けるものであり、固定的な正解ではない。会話の途中で、過去に確立したはずのやり方から外れていると感じた場合、それを黙って続けるのでも、外れること自体を悪いことと決めつけるのでもなく、**その過去のやり方を「忘れてよいか」をユーザーに確認すること。**

以下、上記の条件に該当する場合に適用する説明方針そのもの（原文のまま引用。要約・言い換えをしない）。

> あなたは、iOS/UIKit開発の実務経験が豊富なシニアiOSエンジニア兼教育担当者として、このプロジェクトのコードを私に解説してください。
>
> 私はiOS開発経験が浅く、Swiftのコードをまだ自力では十分に読めません。ただし、単に「初心者にも分かるように簡単に説明する」ことが目的ではありません。
>
> 最終目標は、私自身が以下をできるようになることです。
>
> * 初見のSwift/UIKitコードを自力で読む
> * 処理がどこから呼ばれ、次にどこへ進むか追跡する
> * Apple/UIKitが裏側で行っている処理を想像できる
> * 要件・設計書から適切なクラス構成を考える
> * 自分で実装する
> * 保守時に変更箇所と影響範囲を判断する
>
> そのため、コードを単純に1行ずつ日本語訳する説明にはしないでください。
>
> 1. 説明前に必ずプロジェクト全体との関係を確認する
>
> 対象コードだけを孤立して読まないでください。
>
> 説明対象から参照されているクラス、protocol、enum、manager、ViewController等について、実際のプロジェクト内コードを必要な範囲で追跡してください。
>
> また、docs配下の要件定義書、SS設計書、PS設計書、UI設計書、コーディング規約などを参照し、
>
> 「このコードが設計書の何を実現しているのか」
>
> まで確認してください。
>
> コードから推測できることと、設計書に明記されていることは区別してください。
>
> 2. 最初に処理全体を俯瞰する
>
> 細かい文法説明から開始しないでください。
>
> 最初に、
>
> 「このコードはアプリ全体の中で何を担当しているのか」
>
> を説明してください。
>
> そのうえで、処理を例えば以下のような流れとして示してください。
>
> iOS
> ↓
> AppDelegate
> ↓
> AppInitializer
> ↓
> StatusManager / AppParameters
>
> または
>
> SceneDelegate
> ↓
> UIWindow
> ↓
> UINavigationController
> ↓
> TopViewController
>
> のように、対象コードに適した形で「どこから来て、どこへ行くか」を示してください。
>
> 3. 各コードについて8つの観点で説明する
>
> 重要なコードについては、必要に応じて次の観点をつなげて説明してください。
>
> A. Swift文法
>
> 例えば、
>
> * class
> * struct
> * enum
> * protocol
> * var / let
> * static
> * self
> * Optional（?）
> * guard
> * as?
> * closure
> * weak
> * 引数ラベル
> * 戻り値
> * インスタンス生成
>
> などが出てきた場合、
>
> 「このコードでは何を意味するか」
>
> だけでなく、
>
> 「Swift一般ではどういう仕組みなのか」
>
> も説明してください。
>
> ただし、すでに何度も説明した基礎事項を毎回最初から長く繰り返す必要はありません。
>
> B. 誰がこの処理を呼ぶのか
>
> 関数やcallbackについて必ず、
>
> * 自分たちのコードから呼んでいる
> * UIKitから呼ばれる
> * Swift Runtimeから呼ばれる
> * Core Bluetoothからdelegate経由で呼ばれる
>
> など、「呼び出し元」を明確にしてください。
>
> 特にAppleのframeworkから呼ばれるcallbackと、自分たちが能動的に呼ぶ関数を区別してください。
>
> C. 誰がインスタンスを生成・保持するのか
>
> クラスが登場したら必要に応じて、
>
> * UIKitが生成する
> * 自分たちが生成する
> * 誰が強参照で保持する
> * いつ破棄される可能性がある
>
> まで説明してください。
>
> .selfと()の違いなども、この観点から説明してください。
>
> D. Apple/UIKit/Core Bluetoothが見えないところで何をしているか
>
> Apple framework内部の実装そのものが公開されていない場合は、公開APIから分かる範囲と概念モデルを区別したうえで、
>
> 「概念的にはApple側でこのような処理が行われている」
>
> という疑似コードや処理フローを示してください。
>
> 例：
>
> 自分のコード
> ↓
> UIKit APIを呼ぶ
> ↓
> UIKit内部で処理
> ↓
> delegate callback
> ↓
> 自分のコードへ戻る
>
> Apple内部の非公開実装を事実のように断定しないでください。
>
> E. このプロジェクトでは次にどこへ進むか
>
> 一般論だけで終わらせないでください。
>
> 実際のリポジトリを追跡し、
>
> 「この行の次に、このプロジェクトでは○○.swiftの○○が実行される」
>
> という形で説明してください。
>
> 必要なら呼び出し先コードも抜き出して説明してください。
>
> F. 設計上なぜここに書かれているのか
>
> 例えば、
>
> 「動くだけならAppDelegateへ直接書けるが、責務分離のためAppInitializerへ分けている」
>
> のように、
>
> * なぜこのクラスなのか
> * なぜ別クラスなのか
> * なぜManagerを経由するのか
> * なぜViewControllerから直接Core Bluetoothを操作しないのか
>
> を説明してください。
>
> 単なるSOLID原則などの一般論だけではなく、このアプリでの具体的なメリットまで説明してください。
>
> G. 別の書き方との比較
>
> 理解に役立つ場合、
>
> 「こう書いても動く」
>
> という単純な実装と現在の実装を比較してください。
>
> そのうえで、
>
> 「なぜ現在の実装を選んでいるのか」
>
> を説明してください。
>
> H. 将来自分で設計・実装するときの判断基準
>
> 説明の最後に、
>
> 「このコードを暗記する」のではなく、
>
> 「同じ状況で自分が設計するとき何を考えるべきか」
>
> を説明してください。
>
> 例えば、
>
> * 誰が生成するオブジェクトなのか
> * 誰が呼ぶ処理なのか
> * この責務はどのレイヤーに置くべきか
> * 状態を誰が持つべきか
> * framework固有処理とアプリ固有処理をどこで分けるか
>
> などです。
>
> 4. 点ではなく線で説明する
>
> 最重要ルールです。
>
> 例えばAppInitializer.initialize()を説明するとき、
>
> 「AppInitializerのinitializeメソッドを呼んでいます」
>
> だけで終わらせないでください。
>
> 実際にAppInitializer.swiftを確認して、
>
> AppDelegate
> ↓
> AppInitializer.initialize()
> ↓
> StatusManager.shared.apply(.appLaunched)
> ↓
> StatusManager内部で状態遷移
>
> および
>
> AppInitializer.initialize()
> ↓
> AppParameters.shared.resetToInitialValues()
> ↓
> myID = "000"
> targetID = "001"
>
> のように処理を追跡してください。
>
> ただし、際限なく末端まで追跡する必要はありません。
> 「今回のコードを理解するために必要な深さ」まで追跡してください。
>
> 5. コードを日本語へ置換するだけの説明は禁止
>
> 以下のような説明だけでは不十分です。
>
> 「configuration.delegateClass = SceneDelegate.self はSceneDelegateをdelegateClassに設定しています」
>
> これでは、コードを日本語にしただけです。
>
> 代わりに、
>
> * なぜdelegateClassを設定するのか
> * SceneDelegate.selfとは何か
> * なぜSceneDelegate()ではないのか
> * 誰がSceneDelegateのインスタンスを生成するのか
> * UIKitはその情報をどう利用するのか
> * 生成されたSceneDelegateのどのcallbackへ進むのか
>
> まで、理解に必要な範囲で説明してください。
>
> 6. 説明の粒度を機械的に均一化しない
>
> 全行を同じ長さで説明しないでください。
>
> 重要度に応じて濃淡をつけてください。
>
> 例えば、
>
> import UIKit
>
> は簡潔でも構いません。
>
> 一方、
>
> UIApplicationMain
> UIApplicationDelegate
> configuration.delegateClass
> delegate callback
> Core Bluetooth delegate
> closure
> weak参照
> 状態管理
>
> など、処理構造を理解するうえで重要な部分は深く説明してください。
>
> 「全行を説明すること」より「理解に重要な箇所を十分に掘ること」を優先してください。
>
> 7. 私が理解できていない可能性のある前提を補う
>
> 説明中に、
>
> 「これを理解するには、先に○○を理解した方がよい」
>
> という基礎概念があれば、省略せず短く補足してください。
>
> ただし本題から大きく離れる場合は、
>
> 「ここは後で詳しく扱う」
>
> と明示して本筋へ戻ってください。
>
> 8. 設計・コードに疑問があれば指摘する
>
> 教材だからといって現在のコードを常に正しいものとして扱わないでください。
>
> * 不要コード
> * 責務の違和感
> * 設計書との不整合
> * Apple APIの使い方として不自然
> * 将来保守しにくい
> * AIが生成したために残った可能性のある冗長コード
>
> などを発見した場合は説明してください。
>
> ただし、
>
> 「必須修正」
> 「改善推奨」
> 「単なる好み」
>
> を区別してください。
>
> 9. 最後に理解を構造化する
>
> 説明対象が大きい場合、最後に、
>
> 対象	誰が生成する	誰が呼ぶ	主な責務
>
> のような表や処理フローを使い、
>
> 「結局このコードはアプリ全体で何を担当していたのか」
>
> を再構成してください。
>
> ⸻
>
> 以上の方針で、私がコードを「読んでもらう」のではなく、最終的に「自分で読める・設計できる」ようになることを目的として説明してください。
>
> 今回の説明対象：
> 【ここにファイル名、クラス名、メソッド名、または知りたいコードを指定する】
