#if DEBUG
/// デバッグ機能の有効／無効フラグを一元管理する中心核（PS設計書 付録A「デバッグ機能」）。
///
/// 今後デバッグ機能を追加する場合も、フラグの定義は個々のManagerやViewControllerに
/// 分散させず、このファイルに集約する。こうすることで「現在どのデバッグ機能が
/// 有効になっているか」を1箇所（このファイル）だけを見れば把握・調整できる。
///
/// Release／配布（TestFlight含む）ビルドではこの`#if DEBUG`ブロックごと
/// コンパイル対象から除外されるため、正式仕様には一切影響しない。
enum DebugSettings {
    /// プレビュー確認用バイパス（PS設計書 付録A.1）：
    /// true にすると、通信相手の実機なしでBluetooth接続画面より先の画面を確認できる。
    /// 既定値はfalse。ソースコード上で手動でtrue/falseを切り替えて使用する
    /// （実行中に切り替えるUIは設けない。切り替えるたびにビルド・再実行が必要）。
    /// ⚠️ 実機2台での通信確認を行う際は、必ずfalseに戻してから実行すること。
    static var isPreviewBypassEnabled = false

    // 今後デバッグ機能を追加する場合は、ここに static var を追加していく。
}
#endif
